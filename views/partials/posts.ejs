<section id="posts">
  <h2>Posts</h2>
  <% if (posts.length > 0) { %>
    <% posts.forEach((post) => { %>
      <%- include('./post', { currentUser, post, utils }) %>
    <% }); %>
  <% } else { %>
    <p>Be the first to post!</p>
  <% } %>
</section>